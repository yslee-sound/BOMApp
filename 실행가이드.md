# ANC 자동 설계 시스템 - 실행 가이드

## 🎯 프로젝트 완성!

UI를 시험해볼 수 있는 프로토타입이 완성되었습니다.

### 📂 생성된 파일 구조

```
260211_BOM App/
├── README.md                 # 프로젝트 개요
├── 구현계획서.md             # 상세 구현 계획
├── QUICKSTART.md            # 상세 사용 가이드
├── start-backend.ps1        # 백엔드 실행 스크립트
├── start-frontend.ps1       # 프론트엔드 실행 스크립트
│
├── anc-backend/             # FastAPI 백엔드
│   ├── app/
│   │   ├── main.py          # API 메인
│   │   ├── api/endpoints/   # API 엔드포인트
│   │   ├── models/          # DB 모델 (SQLAlchemy)
│   │   ├── schemas/         # Pydantic 스키마
│   │   ├── services/        # 핵심 알고리즘
│   │   │   ├── layout_service.py   # VS/SPK 자동 배치
│   │   │   └── bom_service.py      # BOM 계산
│   │   └── core/            # DB 설정
│   └── requirements.txt     # Python 의존성
│
└── anc-frontend/            # React 프론트엔드
    ├── src/
    │   ├── App.tsx          # 메인 앱
    │   ├── components/      # UI 컴포넌트
    │   │   ├── ProjectList.tsx      # 프로젝트 목록
    │   │   ├── HouseList.tsx        # 세대 목록
    │   │   ├── SurveyForm.tsx       # 실사 입력
    │   │   ├── DesignView.tsx       # 설계 화면
    │   │   ├── FloorPlanEditor.tsx  # 도면 편집기
    │   │   └── BOMTable.tsx         # BOM 테이블
    │   ├── services/api.ts  # API 통신
    │   └── types/index.ts   # TypeScript 타입
    └── package.json         # NPM 의존성
```

---

## 🚀 실행 방법

### ⚠️ 사전 요구사항

시스템에 다음 프로그램이 설치되어 있어야 합니다:

#### 1. Python 3.11+ 설치
Windows에서 Python 설치:
```powershell
# Python 다운로드
https://www.python.org/downloads/

# 또는 winget으로 설치
winget install Python.Python.3.11

# 설치 확인
python --version
```

#### 2. Node.js 18+ 설치
```powershell
# Node.js 다운로드
https://nodejs.org/

# 또는 winget으로 설치
winget install OpenJS.NodeJS

# 설치 확인
node --version
npm --version
```

---

## 📝 단계별 실행

### Step 1: Backend 실행

**PowerShell을 관리자 권한으로 열고:**

```powershell
# 1. 백엔드 디렉토리로 이동
cd "d:\Workspace\260211_BOM App\anc-backend"

# 2. Python 의존성 설치
pip install fastapi uvicorn sqlalchemy numpy scipy pydantic python-multipart

# 3. 서버 실행
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

✅ **성공 확인:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

🌐 **API 접속:**
- API: http://localhost:8000
- API 문서: http://localhost:8000/docs

---

### Step 2: Frontend 실행

**새 PowerShell 창을 열고:**

```powershell
# 1. 프론트엔드 디렉토리로 이동
cd "d:\Workspace\260211_BOM App\anc-frontend"

# 2. NPM 의존성 설치 (최초 1회)
npm install

# 3. 개발 서버 실행
npm start
```

✅ **성공 확인:**
```
Compiled successfully!

You can now view anc-frontend in the browser.

  Local:            http://localhost:3000
```

🌐 **웹 접속:** http://localhost:3000

---

## 🎮 사용 시나리오

### 1️⃣ 프로젝트 생성
1. 웹 브라우저에서 http://localhost:3000 접속
2. "새 프로젝트" 버튼 클릭
3. 프로젝트명: `테스트 아파트`
4. 위치: `서울시 강남구`
5. "생성" 클릭

### 2️⃣ 세대 추가
1. 생성한 프로젝트 클릭
2. "세대 추가" 버튼 클릭
3. 세대번호: `101동 1001호`
4. 평형: `84㎡`
5. "추가" 클릭

### 3️⃣ 실사 입력
1. 추가한 세대 클릭
2. **거실 가로**: `4500` (mm)
3. **거실 세로**: `3600` (mm)
4. 천장 높이: `2400` (선택사항)
5. 에어컨 있음 체크
6. "저장 및 설계 생성" 클릭

### 4️⃣ 설계 확인
- ✅ **자동 배치 완료!**
  - Vibration Sensor 9개 (3×3 그리드)
  - Speaker 12개 (우물천장 라인)
  
### 5️⃣ 도면 편집
1. 센서/스피커를 **드래그**하여 위치 조정
2. "변경사항 저장" 클릭
3. BOM 자동 재계산

### 6️⃣ BOM 확인
1. "BOM" 탭 클릭
2. 자재 명세서 확인:
   - Vibration Sensor: 9개
   - Speaker: 12개
   - 케이블: 자동 계산
   - 총 비용: 자동 합산

---

## 🎨 화면 구성

### 1. 프로젝트 목록
- 모든 프로젝트 확인
- 프로젝트 추가/삭제
- 세대 수 표시

### 2. 세대 목록
- 프로젝트별 세대 관리
- 실사 완료/설계 완료 상태 표시
- 세대 추가/삭제

### 3. 실사 입력 폼
- 거실 사이즈 입력 (필수)
- 천장 정보 입력
- 장애물 정보 (체크박스)
- 비고 입력

### 4. 설계 화면
- **도면 편집 탭**
  - Konva 캔버스 기반 편집
  - 드래그 앤 드롭
  - 그리드 표시
  - 실시간 좌표 업데이트
  
- **BOM 탭**
  - 자재 명세서 테이블
  - 총 케이블 길이
  - 총 비용 표시

---

## 🔧 주요 기능

### ✅ 구현 완료
- [x] 프로젝트 CRUD
- [x] 세대 CRUD
- [x] 실사 데이터 입력
- [x] 자동 설계 (VS 9개, SPK 12개)
- [x] 도면 시각화 (Konva)
- [x] 드래그 앤 드롭 편집
- [x] BOM 자동 계산
- [x] 케이블 길이 산출
- [x] 실시간 업데이트
- [x] 반응형 UI (Material-UI)

### 🔄 알고리즘
1. **VS 배치**: 3×3 그리드, 300mm 오프셋
2. **SPK 배치**: Edge 기반, 12개 균등 분산
3. **간섭 회피**: 장애물과 최소 거리 유지
4. **케이블 계산**: 맨해튼 거리, 규격 자동 선택

---

## 🐛 문제 해결

### Python 관련
```powershell
# Python 경로 확인
where.exe python

# pip 업그레이드
python -m pip install --upgrade pip

# 가상환경 사용 (권장)
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### Node.js 관련
```powershell
# NPM 캐시 클리어
npm cache clean --force

# node_modules 재설치
Remove-Item -Recurse -Force node_modules
npm install
```

### 포트 충돌
```powershell
# 포트 사용 확인
netstat -ano | findstr :8000
netstat -ano | findstr :3000

# 프로세스 종료
Stop-Process -Id <PID> -Force
```

### CORS 오류
Backend의 `app/main.py`에서 CORS 설정 확인:
```python
allow_origins=["http://localhost:3000"]
```

---

## 📊 API 테스트

Swagger UI에서 API 직접 테스트:
http://localhost:8000/docs

### 테스트 시나리오
1. `POST /api/v1/projects` - 프로젝트 생성
2. `POST /api/v1/projects/{id}/houses` - 세대 추가
3. `POST /api/v1/houses/{id}/survey` - 실사 입력
4. `POST /api/v1/houses/{id}/design/auto` - 자동 설계
5. `GET /api/v1/designs/{id}/bom` - BOM 조회

---

## 📈 다음 단계

현재 프로토타입에 추가 가능:
- [ ] PDF 도면 생성
- [ ] 현장 사진 업로드
- [ ] 장애물 UI 추가 기능
- [ ] 3D 시각화
- [ ] 설계 승인 워크플로우
- [ ] 사용자 인증

---

## 💡 Tips

1. **개발 모드**: 백엔드는 자동 리로드, 프론트엔드는 Hot Reload
2. **데이터 초기화**: `anc.db` 파일 삭제 후 재실행
3. **디버깅**: 
   - Backend: http://localhost:8000/docs
   - Frontend: 브라우저 개발자 도구 (F12)
4. **성능**: 큰 프로젝트는 페이지네이션 추가 권장

---

## 📞 지원

문제 발생 시:
1. 로그 확인 (콘솔 출력)
2. API 문서 참조 (`http://localhost:8000/docs`)
3. `QUICKSTART.md` 참조

완성된 프로토타입으로 UI를 충분히 테스트해보세요! 🎉
