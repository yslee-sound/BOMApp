# ANC 자동 설계 시스템 - 사용자 가이드

## 📌 프로그램 개요

ANC 자동 설계 시스템은 아파트 능동소음제어 시스템의 설계를 자동화하는 웹 애플리케이션입니다.
- 실사 데이터 입력 및 관리
- 자동 설계 도면 생성
- BOM(자재 명세서) 자동 계산

---

## 🚀 프로그램 실행 방법

### 1단계: 사전 준비사항

프로그램 실행 전 다음 소프트웨어가 설치되어 있어야 합니다:

#### Python 3.11 이상
```powershell
# 설치 확인
python --version

# 없다면 설치 (winget 사용)
winget install Python.Python.3.11
```

#### Node.js 18 이상
```powershell
# 설치 확인
node --version

# 없다면 설치 (winget 사용)
winget install OpenJS.NodeJS
```

---

### 2단계: 백엔드 서버 실행

**새 PowerShell 터미널을 열고:**

```powershell
# 백엔드 디렉토리로 이동
cd "d:\Workspace\260211_BOM App\anc-backend"

# 서버 실행
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

✅ **실행 성공 확인:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

🌐 **백엔드 접속 주소:**
- API 서버: http://localhost:8000
- API 문서: http://localhost:8000/docs

---

### 3단계: 프론트엔드 서버 실행

**새 PowerShell 터미널을 하나 더 열고:**

```powershell
# 프론트엔드 디렉토리로 이동
cd "d:\Workspace\260211_BOM App\anc-frontend"

# 서버 실행
npm start
```

✅ **실행 성공 확인:**
```
Compiled successfully!

You can now view anc-frontend in the browser.

  Local:            http://localhost:3000
```

🌐 **프론트엔드 접속 주소:**
- 웹 애플리케이션: http://localhost:3000

> 💡 **자동으로 브라우저가 열립니다!** 
> 열리지 않으면 브라우저에서 http://localhost:3000 을 직접 입력하세요.

---

### 4단계: 프로그램 사용

브라우저에서 http://localhost:3000 에 접속하면 다음과 같이 사용할 수 있습니다:

#### 1️⃣ 프로젝트 생성
- "새 프로젝트 생성" 버튼 클릭
- 프로젝트 정보 입력 (이름, 주소 등)

#### 2️⃣ 세대 추가
- 생성된 프로젝트 선택
- "새 세대 추가" 버튼 클릭
- 세대 정보 입력 (동/호수, 평형 등)

#### 3️⃣ 실사 정보 입력
- 세대 선택 후 "실사 입력" 클릭
- 각 방의 크기와 창문 정보 입력
- 저장

#### 4️⃣ 자동 설계
- "자동 설계" 버튼 클릭
- VS/SPK 자동 배치 도면 확인
- 필요시 수동으로 위치 조정

#### 5️⃣ BOM 확인
- "BOM 생성" 버튼 클릭
- 필요한 자재 목록 및 수량 확인

---

## 🛠️ 빠른 실행 스크립트

매번 명령어를 입력하기 번거롭다면 제공된 스크립트를 사용하세요:

### 방법 1: 백엔드 스크립트 실행
```powershell
cd "d:\Workspace\260211_BOM App"
.\start-backend.ps1
```

### 방법 2: 프론트엔드 스크립트 실행
```powershell
cd "d:\Workspace\260211_BOM App"
.\start-frontend.ps1
```

> ⚠️ **주의:** 백엔드와 프론트엔드는 **별도의 터미널**에서 실행해야 합니다!

---

## 🔴 프로그램 종료 방법

### 백엔드 서버 종료
백엔드 터미널에서:
```
Ctrl + C
```

### 프론트엔드 서버 종료
프론트엔드 터미널에서:
```
Ctrl + C
```
확인 메시지가 나오면 `Y` 입력 후 Enter

---

## ❓ 문제 해결

### 백엔드가 실행되지 않을 때

**문제:** `ModuleNotFoundError: No module named 'fastapi'`

**해결:**
```powershell
cd "d:\Workspace\260211_BOM App\anc-backend"
pip install -r requirements.txt
```

---

### 프론트엔드가 실행되지 않을 때

**문제:** `npm: command not found` 또는 의존성 오류

**해결:**
```powershell
cd "d:\Workspace\260211_BOM App\anc-frontend"

# Node.js 재설치 후
npm install

# 그래도 안되면 캐시 삭제 후 재설치
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
```

---

### 포트가 이미 사용 중일 때

**문제:** `Error: listen EADDRINUSE: address already in use :::8000`

**해결 (백엔드 포트 8000):**
```powershell
# 8000 포트 사용 프로세스 찾기
netstat -ano | findstr :8000

# PID 확인 후 프로세스 종료
taskkill /PID [PID번호] /F
```

**해결 (프론트엔드 포트 3000):**
```powershell
# 3000 포트 사용 프로세스 찾기
netstat -ano | findstr :3000

# PID 확인 후 프로세스 종료
taskkill /PID [PID번호] /F
```

---

### 데이터베이스 초기화

프로그램을 처음 시작하거나 데이터를 초기화하고 싶을 때:

```powershell
cd "d:\Workspace\260211_BOM App\anc-backend"

# SQLite 데이터베이스 파일 삭제 (있는 경우)
Remove-Item *.db

# 서버 재시작 (자동으로 새 DB 생성됨)
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

## 📋 체크리스트

프로그램 실행 시 다음을 확인하세요:

- [ ] Python 3.11+ 설치됨
- [ ] Node.js 18+ 설치됨
- [ ] 백엔드 터미널 열고 서버 실행
- [ ] 프론트엔드 터미널 열고 서버 실행
- [ ] 브라우저에서 http://localhost:3000 접속
- [ ] 두 터미널 모두 실행 상태 유지

---

## 💡 추가 정보

### API 문서 확인
백엔드가 실행 중일 때 http://localhost:8000/docs 에서 API 스펙을 확인할 수 있습니다.

### 개발자 도구
브라우저에서 `F12`를 눌러 개발자 도구를 열면:
- 네트워크 요청 확인
- 콘솔 오류 확인
- 디버깅 가능

### 로그 확인
- **백엔드 로그**: 백엔드 터미널에 실시간 표시
- **프론트엔드 로그**: 브라우저 개발자 도구 콘솔

---

## 📞 지원

문제가 계속 발생하면:
1. 터미널의 오류 메시지 확인
2. 브라우저 콘솔의 오류 메시지 확인
3. 두 서버가 모두 실행 중인지 확인
4. 포트 충돌이 없는지 확인

---

**🎉 이제 ANC 자동 설계 시스템을 사용할 준비가 되었습니다!**
