# ANC 자동 설계 시스템 - 무료 클라우드 배포 가이드

## 📌 개요

이 가이드는 ANC 자동 설계 시스템을 무료 클라우드 서비스에 배포하는 방법을 설명합니다.

**배포 후:**
- ✅ 인터넷 어디서나 접속 가능
- ✅ Python/Node.js 설치 불필요
- ✅ HTTPS 자동 제공
- ✅ 자동 업데이트 및 배포

**무료 플랜 제한:**
- 서버가 15분간 사용되지 않으면 sleep 상태로 전환 (재접속 시 30초 대기)
- 월 750시간 무료 (충분함)
- 데이터베이스는 임시 (재배포 시 초기화)

---

## 🚀 방법 1: Render.com 배포 (가장 쉬움, 추천)

### 사전 준비

1. **GitHub 계정** - https://github.com (무료)
2. **Render 계정** - https://render.com (무료)

### Step 1: GitHub 저장소 생성

1. GitHub에 로그인
2. "New repository" 클릭
3. 저장소 이름: `anc-auto-design`
4. Public 또는 Private 선택
5. "Create repository" 클릭

### Step 2: 코드 업로드

**PowerShell에서:**

```powershell
# Git 초기화 (프로젝트 폴더에서)
cd "d:\Workspace\260211_BOM App"

# Git 저장소 초기화
git init

# .gitignore 파일 생성 (이미 있으면 스킵)
# (중요 파일들은 제외됨)

# 모든 파일 추가
git add .

# 커밋
git commit -m "Initial commit"

# GitHub 저장소 연결 (본인의 저장소 URL로 수정)
git remote add origin https://github.com/YOUR_USERNAME/anc-auto-design.git

# 업로드
git branch -M main
git push -u origin main
```

### Step 3: Render에서 배포

1. **Render 로그인** - https://render.com
2. **Dashboard** → **New** → **Web Service** 클릭
3. **Connect GitHub** (처음 한 번만)
4. 생성한 저장소 선택: `anc-auto-design`
5. 설정 입력:
   - **Name**: `anc-auto-design` (원하는 이름)
   - **Region**: Oregon (가장 가까운 지역)
   - **Branch**: `main`
   - **Runtime**: `Docker`
   - **Instance Type**: `Free`
6. **Create Web Service** 클릭

### Step 4: 배포 대기

- 자동으로 빌드 시작 (5~10분 소요)
- 로그에서 진행 상황 확인
- "Your service is live 🎉" 메시지 확인

### Step 5: 접속

배포 완료 후 제공되는 URL로 접속:
```
https://anc-auto-design.onrender.com
```

**완료!** 이제 어디서든 이 URL로 접속 가능합니다!

---

## 🚀 방법 2: Railway.app 배포

### Step 1: Railway 계정 생성

1. https://railway.app 접속
2. GitHub으로 로그인

### Step 2: 프로젝트 생성

1. **New Project** 클릭
2. **Deploy from GitHub repo** 선택
3. GitHub 저장소 연결
4. `anc-auto-design` 저장소 선택

### Step 3: 자동 배포

- Railway가 자동으로 Dockerfile 감지
- 자동 빌드 및 배포 시작
- 5~10분 대기

### Step 4: 도메인 설정

1. 배포된 서비스 클릭
2. **Settings** → **Networking**
3. **Generate Domain** 클릭
4. 생성된 URL 확인:
   ```
   https://anc-auto-design-production.up.railway.app
   ```

**완료!**

---

## 🚀 방법 3: Fly.io 배포

### Step 1: Fly CLI 설치

```powershell
# PowerShell에서
iwr https://fly.io/install.ps1 -useb | iex
```

### Step 2: 로그인

```powershell
fly auth login
```

### Step 3: 앱 생성 및 배포

```powershell
cd "d:\Workspace\260211_BOM App"

# 앱 초기화
fly launch --name anc-auto-design

# 질문 답변:
# - Would you like to copy its configuration to the new app? Yes
# - Choose a region: Tokyo (가장 가까운)
# - Would you like to set up PostgreSQL? No
# - Would you like to deploy now? Yes

# 배포
fly deploy
```

### Step 4: 접속

```
https://anc-auto-design.fly.dev
```

**완료!**

---

## 📊 비교표

| 서비스 | 난이도 | 속도 | 무료 용량 | 추천도 |
|--------|--------|------|-----------|--------|
| **Render** | ⭐ 쉬움 | 보통 | 750시간/월 | ⭐⭐⭐⭐⭐ |
| **Railway** | ⭐⭐ 쉬움 | 빠름 | $5 크레딧/월 | ⭐⭐⭐⭐ |
| **Fly.io** | ⭐⭐⭐ 중간 | 빠름 | 3개 앱 무료 | ⭐⭐⭐ |

**추천: Render.com** - 가장 쉽고 안정적

---

## 🔄 업데이트 및 재배포

코드를 수정한 후:

```powershell
# 변경사항 커밋
git add .
git commit -m "Update: description of changes"
git push

# Render/Railway는 자동으로 재배포됨!
```

---

## ⚙️ 환경 변수 설정 (선택사항)

### Render에서:
1. Dashboard → 서비스 선택
2. **Environment** 탭
3. 환경 변수 추가:
   - `DATABASE_URL`: PostgreSQL URL (영구 저장용)
   - `SECRET_KEY`: 보안 키

---

## 🗄️ 영구 데이터 저장 (고급)

현재는 SQLite 사용 → 재배포 시 데이터 삭제

**해결: PostgreSQL 사용**

### Render에서 무료 PostgreSQL 생성:

1. **New** → **PostgreSQL**
2. 무료 플랜 선택
3. DATABASE_URL 복사
4. Web Service 환경 변수에 추가

### 백엔드 코드 수정:

```python
# app/core/database.py
import os

DATABASE_URL = os.environ.get(
    "DATABASE_URL",
    "sqlite:///./anc.db"
)

# PostgreSQL용으로 URL 수정
if DATABASE_URL.startswith("postgres://"):
    DATABASE_URL = DATABASE_URL.replace("postgres://", "postgresql://", 1)
```

---

## 🔒 보안 설정

### CORS 설정 업데이트:

[anc-backend/app/main.py](anc-backend/app/main.py)에서:

```python
# 프로덕션 도메인으로 제한
allowed_origins = os.environ.get(
    "ALLOWED_ORIGINS",
    "*"
).split(",")

app.add_middleware(
    CORSMiddleware,
    allow_origins=allowed_origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

환경 변수 설정:
```
ALLOWED_ORIGINS=https://anc-auto-design.onrender.com
```

---

## 📱 커스텀 도메인 (선택사항)

### Render에서:

1. 도메인 구매 (예: Namecheap, GoDaddy)
2. Render Dashboard → 서비스 선택
3. **Settings** → **Custom Domains**
4. "Add Custom Domain" 클릭
5. DNS 설정 안내 따라 하기

---

## 🐛 문제 해결

### 빌드 실패

**원인:** Dockerfile 오류

**해결:**
1. Render 로그 확인
2. 로컬에서 Docker 테스트:
   ```powershell
   docker build -t anc-test .
   docker run -p 8000:8000 anc-test
   ```

### 서버 응답 없음

**원인:** Sleep 상태

**해결:** 첫 접속 시 30초 대기 (무료 플랜 특성)

### API 호출 실패

**원인:** CORS 설정

**해결:** 환경 변수에 프론트엔드 URL 추가

---

## 💰 비용

**완전 무료 조건:**
- Render: 월 750시간 (24/7 운영 가능)
- Railway: 월 $5 크레딧 (소규모 앱 충분)
- Fly.io: 3개 앱까지 무료

실제 사용량이 적으면 **평생 무료** 사용 가능!

---

## ✅ 배포 체크리스트

- [ ] GitHub 저장소 생성
- [ ] 코드 업로드 (`git push`)
- [ ] Render/Railway 계정 생성
- [ ] 저장소 연결
- [ ] 자동 배포 확인
- [ ] URL 접속 테스트
- [ ] 모든 기능 테스트
- [ ] 팀원들과 URL 공유

---

## 🎉 완료!

이제 팀원들에게 URL만 공유하면 됩니다:

```
https://anc-auto-design.onrender.com
```

설치 없이 바로 사용 가능합니다!

---

## 📞 추가 지원

- Render 문서: https://render.com/docs
- Railway 문서: https://docs.railway.app
- Fly.io 문서: https://fly.io/docs

---

**💡 팁:** 처음 배포 시 Render.com 추천 (가장 쉽고 안정적)
