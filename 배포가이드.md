# ANC 자동 설계 시스템 - 배포 가이드

## 📦 독립 실행형 애플리케이션 빌드

이 가이드는 Python이나 Node.js 설치 없이 실행 가능한 독립 실행형 애플리케이션을 만드는 방법을 설명합니다.

---

## 🎯 배포 전략

프론트엔드를 정적 HTML/JS로 빌드하고, 백엔드에서 이를 서빙하도록 한 후, 전체를 하나의 exe 파일로 패키징합니다.

**결과물:** `ANC-AutoDesign.exe` (약 150~200MB)

---

## 🛠️ 1단계: 필수 도구 설치

배포 빌드를 만들려면 다음 도구가 필요합니다:

```powershell
# PyInstaller 설치
pip install pyinstaller

# 기타 필요한 패키지 확인
pip install -r anc-backend\requirements.txt
```

---

## 🚀 2단계: 빌드 실행

### 자동 빌드 (권장)

```powershell
# 프로젝트 루트 디렉토리에서
.\build-standalone.ps1
```

이 스크립트는 다음 작업을 자동으로 수행합니다:
1. 프론트엔드 빌드 (React → 정적 파일)
2. 빌드된 파일을 백엔드에 복사
3. PyInstaller로 exe 파일 생성
4. 배포 패키지 구성

### 수동 빌드

#### Step 1: 프론트엔드 빌드

```powershell
cd anc-frontend
npm install
npm run build
```

생성된 `build` 폴더를 `anc-backend/static`으로 복사:

```powershell
# anc-backend/static 폴더 생성
New-Item -ItemType Directory -Force -Path "anc-backend\static"

# 빌드 파일 복사
Copy-Item -Recurse -Force "anc-frontend\build\*" "anc-backend\static\"
```

#### Step 2: 백엔드 수정 (정적 파일 서빙)

백엔드가 정적 파일을 서빙하도록 이미 설정되어 있어야 합니다.

#### Step 3: PyInstaller로 exe 생성

```powershell
cd anc-backend

# PyInstaller 실행
pyinstaller --name="ANC-AutoDesign" `
    --onefile `
    --windowed `
    --icon=icon.ico `
    --add-data="static;static" `
    --hidden-import=uvicorn.logging `
    --hidden-import=uvicorn.loops `
    --hidden-import=uvicorn.loops.auto `
    --hidden-import=uvicorn.protocols `
    --hidden-import=uvicorn.protocols.http `
    --hidden-import=uvicorn.protocols.http.auto `
    --hidden-import=uvicorn.protocols.websockets `
    --hidden-import=uvicorn.protocols.websockets.auto `
    --hidden-import=uvicorn.lifespan `
    --hidden-import=uvicorn.lifespan.on `
    app/main.py
```

---

## 📂 3단계: 빌드 결과물 확인

빌드가 완료되면 다음 위치에 파일이 생성됩니다:

```
anc-backend/
  └── dist/
      └── ANC-AutoDesign.exe    ← 이 파일을 배포
```

---

## 📋 4단계: 배포 패키지 구성

### 배포 폴더 구조

```
ANC-AutoDesign/
  ├── ANC-AutoDesign.exe        # 실행 파일
  ├── 사용자가이드.txt            # 사용 설명서
  └── README.txt                # 시작하기 안내
```

### README.txt 내용 예시

```
========================================
ANC 자동 설계 시스템
========================================

■ 실행 방법
  1. ANC-AutoDesign.exe를 더블클릭
  2. 자동으로 브라우저가 열림 (http://localhost:8000)
  3. 브라우저가 자동으로 열리지 않으면 수동으로 접속

■ 종료 방법
  - 시스템 트레이에서 아이콘 우클릭 → 종료
  - 또는 작업 관리자에서 프로세스 종료

■ 주의사항
  - 프로그램 실행 중 exe 파일을 옮기지 마세요
  - 8000번 포트가 이미 사용 중이면 실행되지 않습니다

■ 문제 해결
  - 실행 안됨: Windows Defender 예외 추가 필요
  - 방화벽 차단: 8000번 포트 허용 필요
```

---

## 🔧 5단계: 백엔드 코드 수정 (정적 파일 서빙)

`anc-backend/app/main.py` 파일을 다음과 같이 수정해야 합니다:

```python
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse
import os

app = FastAPI(title="ANC Auto Design API")

# API 라우터 등록
from app.api.endpoints import projects, houses, surveys, designs

app.include_router(projects.router, prefix="/api/projects", tags=["projects"])
app.include_router(houses.router, prefix="/api/houses", tags=["houses"])
app.include_router(surveys.router, prefix="/api/surveys", tags=["surveys"])
app.include_router(designs.router, prefix="/api/designs", tags=["designs"])

# 정적 파일 서빙 (프론트엔드)
static_dir = os.path.join(os.path.dirname(__file__), "..", "static")

if os.path.exists(static_dir):
    app.mount("/static", StaticFiles(directory=static_dir), name="static")
    
    @app.get("/")
    async def serve_frontend():
        return FileResponse(os.path.join(static_dir, "index.html"))
    
    # React Router를 위한 fallback
    @app.get("/{full_path:path}")
    async def serve_frontend_routes(full_path: str):
        file_path = os.path.join(static_dir, full_path)
        if os.path.exists(file_path) and os.path.isfile(file_path):
            return FileResponse(file_path)
        return FileResponse(os.path.join(static_dir, "index.html"))
```

---

## 💡 6단계: 고급 옵션

### 자동 브라우저 실행

프로그램 시작 시 자동으로 브라우저를 여는 기능 추가:

```python
import webbrowser
import threading

def open_browser():
    import time
    time.sleep(1.5)  # 서버 시작 대기
    webbrowser.open('http://localhost:8000')

# main.py의 시작 부분에 추가
if __name__ == "__main__":
    threading.Thread(target=open_browser, daemon=True).start()
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

### 시스템 트레이 아이콘 (선택사항)

`pystray` 라이브러리를 사용하여 시스템 트레이에 아이콘 추가 가능:

```powershell
pip install pystray pillow
```

---

## 📊 예상 파일 크기

- **exe 파일**: 150~200MB
  - Python 런타임: ~50MB
  - 라이브러리들: ~70MB
  - 프론트엔드 정적 파일: ~10MB
  - 기타: ~20MB

---

## ⚠️ 주의사항

### 1. Windows Defender

처음 실행 시 Windows Defender가 경고를 표시할 수 있습니다:
- "SmartScreen이 인식되지 않는 앱의 시작을 차단했습니다"
- **해결**: "추가 정보" → "실행" 클릭

### 2. 방화벽

Windows 방화벽이 네트워크 액세스를 차단할 수 있습니다:
- **해결**: 액세스 허용 선택

### 3. 바이러스 백신

일부 백신 프로그램이 PyInstaller로 만든 exe를 오진할 수 있습니다:
- **해결**: 예외 목록에 추가

---

## 🔄 업데이트 배포

새 버전을 배포할 때:

1. 이전 버전 exe 파일 삭제를 사용자에게 안내
2. 새 exe 파일 배포
3. 데이터베이스 파일(*.db)은 유지하도록 안내

---

## 📤 최종 배포

### ZIP 파일로 배포

```powershell
# 배포 폴더 생성
New-Item -ItemType Directory -Force -Path "ANC-Release"

# 필요한 파일 복사
Copy-Item "anc-backend\dist\ANC-AutoDesign.exe" "ANC-Release\"
Copy-Item "사용자가이드.md" "ANC-Release\사용자가이드.txt"

# ZIP으로 압축
Compress-Archive -Path "ANC-Release\*" -DestinationPath "ANC-AutoDesign-v1.0.zip"
```

### 설치 프로그램 제작 (선택사항)

Inno Setup 또는 NSIS를 사용하여 설치 프로그램을 만들 수 있습니다.

---

## ✅ 테스트 체크리스트

배포 전 반드시 확인:

- [ ] exe 파일이 정상적으로 실행되는가?
- [ ] 브라우저가 자동으로 열리는가?
- [ ] 모든 API가 정상 작동하는가?
- [ ] 프론트엔드 화면이 정상적으로 표시되는가?
- [ ] 데이터 입력/저장/조회가 정상 작동하는가?
- [ ] 프로그램 종료가 정상적으로 되는가?
- [ ] 재실행 시 이전 데이터가 유지되는가?

---

## 🆘 문제 해결

### exe 파일이 너무 크다

- `--onefile` 대신 `--onedir` 옵션 사용
- 불필요한 라이브러리 제거

### 실행 속도가 느리다

- `--onedir` 옵션 사용 (압축 해제 시간 단축)
- UPX로 압축 (속도 개선)

### 특정 기능이 작동하지 않는다

- 누락된 데이터 파일이 있는지 확인
- `--add-data` 옵션으로 필요한 파일 추가

---

**🎉 이제 독립 실행형 애플리케이션을 배포할 준비가 되었습니다!**
