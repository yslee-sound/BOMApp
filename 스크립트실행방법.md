# PowerShell 스크립트(.ps1) 실행 방법

## 🎯 가장 쉬운 방법

### 방법 1: 우클릭으로 실행

1. **파일 탐색기**에서 `build-standalone.ps1` 파일을 찾습니다
2. 파일에서 **마우스 우클릭**
3. **"PowerShell에서 실행"** 선택

> 💡 이 옵션이 안 보이면 방법 2를 사용하세요

---

### 방법 2: PowerShell에서 직접 실행

#### Step 1: PowerShell 열기

**방법 A - 파일 탐색기에서:**
1. 파일 탐색기에서 `d:\Workspace\260211_BOM App` 폴더 열기
2. 주소창에 `powershell` 입력 후 Enter
3. PowerShell이 현재 폴더에서 열림

**방법 B - 시작 메뉴에서:**
1. `Windows + X` 누르기
2. "Windows PowerShell" 선택
3. 다음 명령어 입력:
   ```powershell
   cd "d:\Workspace\260211_BOM App"
   ```

#### Step 2: 스크립트 실행

```powershell
.\build-standalone.ps1
```

---

## ⚠️ 보안 오류가 발생하면?

### 오류 메시지:
```
이 시스템에서 스크립트를 실행할 수 없으므로 ...
```

### 해결 방법:

**임시로 허용 (권장):**
```powershell
PowerShell -ExecutionPolicy Bypass -File .\build-standalone.ps1
```

**영구적으로 허용:**
```powershell
# 관리자 권한 PowerShell 열기
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# 확인 메시지에서 Y 입력
```

그 후 다시 실행:
```powershell
.\build-standalone.ps1
```

---

## 📝 단계별 상세 가이드

### 전체 과정:

```powershell
# 1. PowerShell 열기 (Windows + X → Windows PowerShell)

# 2. 프로젝트 폴더로 이동
cd "d:\Workspace\260211_BOM App"

# 3. 실행 정책 확인 (오류 방지)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 4. 스크립트 실행
.\build-standalone.ps1
```

---

## 🎬 VS Code에서 실행 (현재 환경)

현재 VS Code를 사용 중이시라면:

1. **터미널 열기**: `` Ctrl + ` `` (백틱 키)
2. **명령어 입력**:
   ```powershell
   .\build-standalone.ps1
   ```

---

## 💡 각 스크립트별 실행 방법

### 백엔드 서버 실행:
```powershell
.\start-backend.ps1
```

### 프론트엔드 서버 실행:
```powershell
.\start-frontend.ps1
```

### 독립 실행형 빌드:
```powershell
.\build-standalone.ps1
```

---

## 🔍 문제 해결

### "명령을 찾을 수 없습니다"

**원인:** 현재 폴더가 아닙니다

**해결:**
```powershell
# 현재 위치 확인
pwd

# 올바른 폴더로 이동
cd "d:\Workspace\260211_BOM App"
```

### "액세스가 거부되었습니다"

**원인:** 실행 권한이 없습니다

**해결:**
```powershell
# 관리자 권한 PowerShell 실행
# Windows + X → Windows PowerShell (관리자)

# 실행 정책 변경
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 스크립트가 중간에 멈춤

**원인:** 오류 발생

**해결:** 오류 메시지를 확인하고:
- Python/Node.js 설치 확인
- 의존성 설치 확인
- 포트 사용 여부 확인

---

## ✅ 빠른 체크리스트

실행하기 전 확인:

- [ ] PowerShell이 설치되어 있음 (Windows 기본 설치)
- [ ] 올바른 폴더에 있음 (`d:\Workspace\260211_BOM App`)
- [ ] 스크립트 파일이 존재함 (`.ps1` 파일)
- [ ] 실행 정책이 허용됨

---

## 🎯 한눈에 보는 명령어 모음

```powershell
# 폴더 이동
cd "d:\Workspace\260211_BOM App"

# 실행 권한 설정 (한 번만)
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# 빌드 실행
.\build-standalone.ps1

# 또는 권한 없이 바로 실행
PowerShell -ExecutionPolicy Bypass -File .\build-standalone.ps1
```

---

**🎉 이제 PowerShell 스크립트를 실행할 수 있습니다!**
